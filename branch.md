`git branch` to list
`git checkout -b my_branch` to create branch
`git checkout my_branch` to switch to existing branch

create pull request and merge branch to main
en general je crois, une bonne pratique c'est de creer une brache par petite tache, merge souvent et fermer chaque branche une fois inutile pour eviter des conflits ou incoherences
